<%= form_with(model: training) do |form| %>
  <% if training.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(training.errors.count, "error") %> prohibited this training from being saved:</h2>

      <ul>
        <% training.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :credit, style: "display: block" %>
    <%= form.number_field :credit %>
  </div>
  <div>
    <%= form.collection_check_boxes :course_ids, Course.all, :id, :name, { hide_label: true } %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>

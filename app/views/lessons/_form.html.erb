<%= form_with(model: lesson) do |form| %>
  <% if lesson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lesson.errors.count, "error") %> prohibited this lesson from being saved:</h2>

      <ul>
        <% lesson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :materials, style: "display: block" %>
    <%= form.file_field :materials, multiple: true %>
  </div>

  <div>
    <%= form.label :course_module_id, style: "display: block" %>
 <%= form.collection_select :course_module_id, CourseModule.all, :id, :title %>
   
  </div>



  <%= form.label :products %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
